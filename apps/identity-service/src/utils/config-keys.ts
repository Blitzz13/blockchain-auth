export const JWT_CONFIG_KEYS = {
  ACCESS_SECRET: 'jwt_config.access.secret',
  ACCESS_EXPIRES_IN: 'jwt_config.access.expiresIn',
  REFRESH_SECRET: 'jwt_config.refresh.secret',
  REFRESH_EXPIRES_IN: 'jwt_config.refresh.expiresIn',
};
