import { ETHERS_CONFIG_TOKEN } from '../configs/ethers.config';

export const ETHERS_CONFIG_KEYS = {
  ETHERS_HTTP_URL: `${ETHERS_CONFIG_TOKEN}.httpUrl`,
  ETHERS_WSS_URL: `${ETHERS_CONFIG_TOKEN}.wssUrl`,
};
