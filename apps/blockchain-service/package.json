{
  "name": "@blockchain-auth/blockchain-service",
  "version": "0.2.0",
  "private": true,
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "node-env=production"
          ]
        },
        "configurations": {
          "development": {
            "args": [
              "node-env=development"
            ]
          }
        }
      },
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@blockchain-auth/blockchain-service:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@blockchain-auth/blockchain-service:build:development"
          },
          "production": {
            "buildTarget": "@blockchain-auth/blockchain-service:build:production"
          }
        }
      },
      "test": {
        "executor": "@nx/jest:jest",
        "outputs": [
          "{workspaceRoot}/coverage/{projectRoot}"
        ],
        "options": {
          "jestConfig": "apps/blockchain-service/jest.config.ts"
        }
      },
      "test:coverage": {
        "executor": "@nx/jest:jest",
        "outputs": [
          "{workspaceRoot}/coverage/{projectRoot}"
        ],
        "options": {
          "jestConfig": "apps/blockchain-service/jest.config.ts",
          "codeCoverage": true,
          "coverageReporters": [
            "html",
            "text"
          ]
        }
      },
      "docker-build": {
        "dependsOn": [
          "build"
        ],
        "command": "VERSION=$(node -p \"require('./apps/blockchain-service/package.json').version\") && docker build -f apps/blockchain-service/Dockerfile . -t blockchain-auth-blockchain-service:$VERSION -t blockchain-auth-blockchain-service:latest"
      }
    }
  }
}